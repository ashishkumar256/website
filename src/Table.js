import React from "react";

function Table() {
  return (
    <div>
      <h2>Table {Math.random().toString(36).substring(2, 15)}</h2>
      {/* Put your table structure here */}
    </div>
  );
}

export default Table;
